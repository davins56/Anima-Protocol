# Public Directory

This directory is intentionally present for Vercel project settings that require
a repository-level public directory.

The main Anima Protocol frontend still builds from
`artifacts/anima-protocol` and emits deployable static files to
`artifacts/anima-protocol/dist/public`, which is the output directory configured
in `vercel.json`.
