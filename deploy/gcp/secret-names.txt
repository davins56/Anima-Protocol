# Names only — create these in Secret Manager before the first Cloud Run deploy.
# Never put values in git. Hyperdrive / Workers AI / Secrets Store do not exist
# on GCP; DATABASE_URL is a normal Postgres URL (Supabase session or transaction
# pooler, or Cloud SQL). Chat stays fail-closed without the Worker AI binding.

DATABASE_URL
CLERK_SECRET_KEY
CLERK_PUBLISHABLE_KEY
